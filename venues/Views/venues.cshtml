<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Venues</title>
  </head>
  <body>
    @if(Model.Count==0)
    {
      <p>
        No venues have been added yet
      </p>
    }
    else
    {
      <ul>
      @foreach(var venue in Model)
      {
        <li><a href="/venues/@venue.GetId()">@venue.GetName()</a></li>
      }
    </ul>
    }
    <a href="/venues/add">Add New Venue</a>
    <a href="/">Home</a>
  </body>
</html>
